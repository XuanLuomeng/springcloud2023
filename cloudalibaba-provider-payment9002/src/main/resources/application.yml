server:
  port: 9002

spring:
  application:
    name: nacos-payment-provider
  cloud:
    nacos:
      discovery:
        server-addr: 192.168.190.100:1111/nacos #配置Nacos地址

management:
  endpoints:
    web:
      exposure:
        include: '*'